# this is a readme 


 # to add something into already existing repo
<!-- Always keep in mind that your branch name should be main-> 

if not than - git branch -M main -->
 - git add .
 - git commit -m "commit name"
 - git push -u origin main (once ) 
 <!-- after that  every time just follow -->
 - git add .
 - git commit -m "commit name"
 - git push   